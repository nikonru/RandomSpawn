# Build
## Requirements
To build this project you need `gradle` of version `8.8` and `Java SDK` of `22.0.2` version.

You can check `Java SDK` version with the following command
```
javac -version
```
And `gradle` one with
```
gradle -version
```

## Build Jar
Once everything requirements are satisfied run `gradle` in your repository folder
```
gradle build
```
This command creates a `build/libs` folder in your repository, you can find a `jar` file there.
